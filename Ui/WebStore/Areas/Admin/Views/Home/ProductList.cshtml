@model System.Collections.Generic.IEnumerable<WebStore.DomainNew.Entities.Product>

@{
    ViewData["Title"] = "Список товаров";
}

<table class="table">
    <thead>
        <tr>
            <th>@Html.DisplayNameFor(model => model.Id)</th>
            <th>@Html.DisplayNameFor(model => model.Name)</th>
            <th>@Html.DisplayNameFor(model => model.Order)</th>
            <th>@Html.DisplayNameFor(model => model.SectionId)</th>
            <th>@Html.DisplayNameFor(model => model.BrandId)</th>
            <th>@Html.DisplayNameFor(model => model.ImageUrl)</th>
            <th>@Html.DisplayNameFor(model => model.Price)</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <th>@Html.DisplayFor(m => item.Id)</th>
                <th>@Html.DisplayFor(m => item.Name)</th>
                <th>@Html.DisplayFor(m => item.Order)</th>
                <th>@Html.DisplayFor(m => item.SectionId)</th>
                <th>@Html.DisplayFor(m => item.BrandId)</th>
                <th>@Html.DisplayFor(m => item.ImageUrl)</th>
                <th>@Html.DisplayFor(m => item.Price)</th>
                <th>
                    @Html.ActionLink("Edit", "Edit", new { }) |
                    @Html.ActionLink("Details", "Details", new { }) |
                    @Html.ActionLink("Delete", "Delete", new { })
                    @Html.ActionLink("Delete", "Delete", new { })
                </th>
            </tr>
        }
    </tbody>
</table>